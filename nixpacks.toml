[phases.setup]
nixPkgs = ["nodejs_22", "pnpm"]

[phases.install]
cmds = ["pnpm i --frozen-lockfile --ignore-scripts"]

[phases.build]
cmds = [
    "pnpm --filter web exec prisma generate --schema=./prisma/schema.prisma",
    "pnpm build"
]
[start]
cmd = "pnpm --filter web start"
