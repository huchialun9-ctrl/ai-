{
    "name": "nova-agent-monorepo",
    "private": true,
    "scripts": {
        "dev": "pnpm --filter web dev",
        "build": "pnpm --filter web build",
        "start": "pnpm --filter web start"
    },
    "devDependencies": {
        "typescript": "^5.0.0"
    },
    "engines": {
        "node": ">=20.19.0"
    },
    "pnpm": {
        "overrides": {
            "@types/react": "19.2.13",
            "@types/react-dom": "19.2.3"
        }
    }
}